const colors = require('tailwindcss/colors')

module.exports = {
  // purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    colors: {
      green: colors.coolGray
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
